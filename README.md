# Unsandboxed Extensions
Extensions repository for Unsandboxed.

Currently not accepting or reviewing new extensions.

Will be made public when graduated to release.

# Development
Use `npm run dev` to build a TW-like dev server at `https://localhost:8001/`.

`extensions.json` is built automatically. You do not need to change it.